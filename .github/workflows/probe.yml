name: TEMP probe planilhas
on:
  push:
    branches: [ "claude/**" ]
    paths: [ ".github/workflows/probe.yml", "tools/probe_sheets.py" ]
  workflow_dispatch:
permissions:
  contents: read
jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: { python-version: "3.12" }
      - run: python tools/probe_sheets.py
